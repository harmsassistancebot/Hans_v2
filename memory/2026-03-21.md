# 2026-03-21

## Session notes

- Canonical daily note created automatically at session start.

---

## 2026-03-20/21 Session — Key Events

### Presentations
- Created **Governance Engineering** presentation using Marp and Slidev
- Slidev project lives at: `~/.openclaw/workspace/tmp/slidev-gov/slides.md`
- Vault copy: `slidev/Governance Engineering.md`
- Exported PDF: `attachments/Governance Engineering - Slidev.pdf`
- Slidev theme: `default` (Geist was tried and abandoned — didn't match expectations)
- Global CSS injected via `<style>` block after frontmatter (not frontmatter `style:` key — doesn't work globally)
- Arial Black applied to all headings via that `<style>` block
- Failure 2 (Stale & Invisible Policies) was missing from deck and was added back
- ista brand colors: primary `#0055B4`, dark `#0A2864`, font Arial/Arial Black

### PKB Data Contract
- Reviewed versions 1.7 → 1.8 → 1.9 → 2.0 → 2.4 → 2.5 → 2.6
- v2.5: merged global + special field catalogs into one table, added `-hans` suffix rule
- v2.6: added Mermaid chapter (Chapter 7), rest of chapters renumbered
- Key decisions: `categories` is scalar string, `domain` is array, `next_action` removed as standalone field (now a `gtd_folder` value), `memos` (not `memo`)
- Templates still use `get_info` instead of `gtd_info` — not yet fixed
- `ista-teams` subcategory mapped to `references/orgs` in template mapping

### Vault file operations
- `todays_files.sh` — script in vault root listing today's root .md files numbered by mtime
- Trigger phrase: "today's files" → run the script and show output
- **xargs + spaces bug**: always use Python `os.walk` or `find -print0 | xargs -0` for vault file ops; plain `xargs stat` silently drops filenames with spaces
- "files of last N hours" → use Python datetime/os.stat approach
- File #N references the numbered list from most recent file listing

### Git sync script (`auto-sync-vault.sh`)
- Rewrote logging to compact one-liner per sync
- Format: `- **TIMESTAMP** — Synced | pushed: files` or `No changes`
- Fixed macOS `printf` bug with leading `-` (use variable interpolation instead)
- No-op syncs log `No changes`

### PDF → Markdown conversion
- Gartner RAG PDF had intentionally garbled embedded text (DRM)
- Used `pdftoppm` to render pages as PNG, then OCR via image tool
- Result saved: `landing_zone_docs/Use These 8 Practices to Improve RAG Systems.md`
- For future PDFs: `pdftotext` works for clean PDFs, OCR path needed for DRM/image PDFs

### CMDB Server Tagging Concept
- Cleaned up to v1.3 (compacted tables, fixed terminology)
- Converted to PDF and DOCX: `attachments/Server Tagging Concept.pdf/.docx`
- Mermaid → PNG via `mmdc`, then pandoc → HTML → Chrome headless → PDF
- Marp also available for Markdown → PDF

### Software Approval: Obsidian
- Created `Software Approval Justification for Obsidian-hans.md`
- Original had typo: `Software Approval Justification for Obisidian.md`
- New version argues: open format, no vendor lock-in, knowledge structuring, AI collaboration

### Meeting cleanup
- `2026-03-20 Meeting with Microsoft on Oracle.md` cleaned up (structure, typos, tags)
- Oracle Database@Azure: POC planned, wait until April 1 for new MACC enrollment

### Morning Briefing
- Added deduplication via `briefing/seen-stories.json`
- Rolling 7-day window; stories matched by URL or fuzzy title
- Agent writes used stories back to JSON after each briefing

### Model
- Session model switched to Sonnet (`anthropic/claude-sonnet-4-6`) by Jan's request

### Rules confirmed
- `-hans` suffix on all files I create autonomously (unless Jan says to edit original directly)
- "today's files" → run `todays_files.sh` and show list
- When Jan says "review N" → file N from the last listing
