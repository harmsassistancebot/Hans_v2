# 2026-03-18

## Related notes
- [[2026-03-18-brave-api-setup]]

## Session notes

### Vault: todays_files.sh
- Created `todays_files.sh` in vault root (`~/ObsidianVaults/Jan/`)
- Lists today's `.md` files in vault root only (no subfolders), numbered by modification time
- When Jan says "today's files" or similar → run this script and show the output directly

### PDF import: Gartner RAG note
- Converted `attachments/Use These 8 Practices to Improve RAG Systems.pdf` to Markdown
- Saved as `landing_zone_docs/Use These 8 Practices to Improve RAG Systems.md`
- PDF had intentionally garbled embedded text (Gartner DRM); used OCR/page-image transcription instead

### GitSync.md logging
- Rewrote `auto-sync-vault.sh` to log compact one-liners instead of full git output
- Format: `- **TIMESTAMP** — Synced | pushed: file1, file2`
- No-op syncs log `No changes`
- Fixed macOS `printf` bug with leading `-` in log lines

### AGENTS.md startup fix
- Updated startup sequence to handle missing daily memory files gracefully
- Rule: if `memory/YYYY-MM-DD.md` is missing, create it; if date-specific notes exist, link them
- Created canonical stubs for 2026-03-17 and 2026-03-18

### xargs + spaces bug
- `xargs stat` silently drops filenames with spaces — always use Python or `find -print0 | xargs -0` for vault file operations
